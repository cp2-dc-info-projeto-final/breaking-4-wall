CREATE TABLE Atores (
    ID INT PRIMARY KEY AUTO_INCREMENT,
    Nome VARCHAR(255) NOT NULL,
    DataNascimento DATE,
    Nacionalidade VARCHAR(50),
    Genero VARCHAR(20),
    Biografia TEXT
);

CREATE TABLE Filmes (
    ID INT PRIMARY KEY AUTO_INCREMENT,
    Titulo VARCHAR(255) NOT NULL,
    AnoLancamento YEAR,
    Diretor VARCHAR(255),
    Sinopse TEXT
);

CREATE TABLE Atuacoes (
    ID INT PRIMARY KEY AUTO_INCREMENT,
    AtorID INT,
    FilmeID INT,
    FOREIGN KEY (AtorID) REFERENCES Atores(ID),
    FOREIGN KEY (FilmeID) REFERENCES Filmes(ID)
);

INSERT INTO Atores (Nome, DataNascimento, Nacionalidade, Genero, Biografia)
VALUES ('Nome do Ator', '1990-01-01', 'Nacionalidade', 'Masculino', 'Biografia do ator.');

INSERT INTO Filmes (Titulo, AnoLancamento, Diretor, Sinopse)
VALUES ('Título do Filme', 2023, 'Diretor do Filme', 'Sinopse do filme.');

INSERT INTO Atuacoes (AtorID, FilmeID)
VALUES (1, 1); -- Isso associa o ator com ID 1 ao filme com ID 1

